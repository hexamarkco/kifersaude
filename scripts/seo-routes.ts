/**
 * Fonte única das rotas públicas que podem ser indexadas.
 * Adicione uma rota somente quando ela tiver conteúdo próprio, canônico e útil.
 */
export const indexablePublicRoutes = ['/'] as const;
